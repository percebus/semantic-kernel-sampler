services:
  rest-app:
    build:
      context: .
      target: rest-app
    command: start
    ports:
      - 8080:3000

  # TODO typescript-app:

  python-app:
    build:
      context: ./python/semantic-kernel-sampler-py
      target: release
    ports:
      - 8082:5000

  dotnet-app:
    build:
      context: ./dotnet
      target: webapp
    ports:
      - 8083:8080
